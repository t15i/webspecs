name: "PR Labeling"
on: 
  pull_request_target:
    types: [opened, reopened, synchronize]
jobs:
  labeler:
    name: "Label"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - id: labeler
        uses: actions/labeler@v5
